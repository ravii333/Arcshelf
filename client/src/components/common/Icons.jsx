// client/src/pages/Icons.jsx

import { 
  MagnifyingGlassIcon as SearchIcon, 
  BookOpenIcon, 
  ArrowUpTrayIcon as UploadIcon, 
  UsersIcon 
} from '@heroicons/react/24/outline';

// Now we directly export the icons. 
// We renamed them during the import process so there are no conflicts.
// Note: We don't need to rename BookOpenIcon and UsersIcon as they are already good names.
export { SearchIcon, BookOpenIcon, UploadIcon, UsersIcon };